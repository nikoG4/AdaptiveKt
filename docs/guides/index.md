# Guides

- `responsive-layouts.md`: responsive layout notes.
- Use `AdaptiveContent` and `rememberAdaptiveInfo` for breakpoint-aware decisions.
- Prefer module-level composition over copying demo-only dependencies into libraries.
